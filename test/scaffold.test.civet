import { describe, it, expect } from "vitest"
import { checkSource } from "../src/engine.civet"

describe "scaffold", ->
  it "initializes engine", ->
    result := checkSource("x := 1")
    expect(result.diagnostics).toEqual([])
