import { describe, it, expect } from "vitest"
import { lintSource } from "../src/engine.civet"

describe "scaffold", ->
  it "initializes engine", ->
    result := lintSource("x := 1")
    expect(result.diagnostics).toEqual([])
